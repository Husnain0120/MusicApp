/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "img.freepik.com"], // Separate the domains with commas
  },
};

export default nextConfig;
